```sql

wget http://labfile.oss.aliyuncs.com/courses/9/createdb.sql


sudo service mysql start

mysql -u root

source /home/shiyanlou/createdb.sql

select s.sid,sname,gender
from student s,mark m,course c
where s.sid = m.sid and cname = 'physics' and c.cid = m.cid
and m.score in (
select max(score)
from mark m,course c
where m.cid = c.cid and cname = 'physics'
)
INTO OUTFILE '/var/lib/mysql-files/physics.txt';


update mark m,student s,course c set m.score = m.score+3 where m.sid = s.sid and s.sname = 'tom' and m.cid = c.cid and c.cname = 'chemistry';

SELECT s.sid,s.sname,s.gender FROM student s,( SELECT m.sid,m.cid,m.score FROM mark m,course c WHERE m.cid = c.cid AND c.cname='physics' ORDER BY m.score DESC LIMIT 1) t WHERE s.sid = t.sid INTO OUTFILE '/var/lib/mysql-files/physics.txt'

```